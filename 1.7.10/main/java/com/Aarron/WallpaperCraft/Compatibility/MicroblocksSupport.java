package com.Aarron.WallpaperCraft.Compatibility;


import net.minecraft.item.ItemStack;
import codechicken.microblock.BlockMicroMaterial;
import codechicken.microblock.MicroMaterialRegistry;

import com.Aarron.WallpaperCraft.Blocks.CoreBlock;

public class MicroblocksSupport {


	public static void init() {	
		BlockMicroMaterial.createAndRegister(CoreBlock.SolidRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.SolidRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.SolidPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.SolidPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.SolidBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.SolidBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.SolidCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.SolidCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.SolidGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.SolidGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.SolidYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.SolidYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.SolidBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.SolidBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.SolidGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.SolidGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DiagonallyDottedGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.StripedRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.StripedRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.StripedPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.StripedPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.StripedBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.StripedBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.StripedCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.StripedCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.StripedGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.StripedGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.StripedYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.StripedYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.StripedBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.StripedBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.StripedGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.StripedGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FloralRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FloralRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FloralPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FloralPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FloralBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FloralBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FloralCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FloralCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FloralGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FloralGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FloralYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FloralYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FloralBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FloralBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FloralGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FloralGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.ClayRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.ClayRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.ClayPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.ClayPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.ClayBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.ClayBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.ClayCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.ClayCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.ClayGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.ClayGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.ClayYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.ClayYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.ClayBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.ClayBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.ClayGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.ClayGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TintedGlassGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.TexGlassGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.FrostedGlassGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.BrickRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.BrickRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.BrickPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.BrickPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.BrickBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.BrickBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.BrickCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.BrickCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.BrickGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.BrickGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.BrickYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.BrickYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.BrickBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.BrickBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.BrickGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.BrickGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.DottedCarpetGray, i);
		}
		
		
		
		
		
		
		BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetRed, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetRed, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetPurple, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetPurple, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetBlue, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetBlue, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetCyan, 0);
		for (int i = 0; i < 9; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetCyan, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetGreen, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetGreen, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetYellow, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetYellow, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetBrown, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetBrown, i);
		}
		
		BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetGray, 0);
		for (int i = 0; i < 15; i ++) {
			BlockMicroMaterial.createAndRegister(CoreBlock.CheckeredCarpetGray, i);
		}
		
		
		
		
		
		
		

	}
}